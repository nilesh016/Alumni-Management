import express from "express";
// Import user-related controllers when needed (like profile update, password reset)

const router = express.Router();

// ✅ User-related routes will go here (e.g., profile, update, delete)

export default router;
